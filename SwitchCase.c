#include<stdio.h>

int main() {
	/* 기본형
		Switch(Integer){
		case integer: 
					(Body)
					break;
		default;
	*/
}

// Switch ~ Case 에서는 break; 를 붙일 때, 효율적으로 사용해야 한다.
// Switch는 기본적으로 아래로 흐른다고 생각하면 되며, 같은 경우는 묶어서 사용 가능하다.